{
  "name": "muslimain-web",
  "private": true,
  "scripts": {
    "start": "npx serve src",
    "deploy": "cp -r src/* dist/ && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "serve": "^14.2.1"
  }
}
